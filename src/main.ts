import { VictronMqttConnectionOptions } from "./models/VictronMqttConnectionOptions.js";

export class VictronMqttConsumer {
  public static async connect(opts: VictronMqttConnectionOptions): boolean {
    // Validate Options
    // Connect to Device
    // Start Keepalive
    return true
  }
}
